#ifndef MODULES_DIRECTORY
    #define MODULES_DIRECTORY node_modules
#endif

class grad_guestbook {
    class common {
        file = MODULES_DIRECTORY\grad-guestbook\functions;

        class addInteraction {};
    };
};
